# Load required packages
library(shiny)

# Add any other packages your app needs
# library(ggplot2)
# library(dplyr)
# library(tidyr)

# Define any global variables or functions
appName <- "Minimal Shiny App"
